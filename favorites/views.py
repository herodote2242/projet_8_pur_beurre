from django.shortcuts import render


def favorite(request):
    """
    This function is responsible of the favorites page, where the user can
    find all the products previously saved. He has access to the detailed page
    when clicking on an product'picture.
    """
    
    return render(request, )